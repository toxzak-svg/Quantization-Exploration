{"code":"import os, time; t0 = time.time(); pid = int(open('/content/pareto_scan.pid').read().strip()) if os.path.exists('/content/pareto_scan.pid') else None; print('time:', time.strftime('%H:%M:%S')); print('pid:', pid); print('alive:', os.system(f'ps -p {pid} > /dev/null 2>&1 && echo YES || echo NO') if pid else 'no pid'); log = '/content/pareto_scan.log'; print('--- log lines:', sum(1 for _ in open(log)) if os.path.exists(log) else 'no log'); print('--- last 50 lines:'); lines = open(log).readlines() if os.path.exists(log) else []; print(''.join(lines[-50:]))"}
