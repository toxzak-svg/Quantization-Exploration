{"code":"import json, os, time; aw='/content/sub1quant/eval_results/act_weighted_scan/activation_weights_gemma4.json'; d=json.load(open(aw)) if os.path.exists(aw) else {}; vals=list(d.values()); print('time:', time.strftime('%H:%M:%S')); print('keys:', len(d)); print('min:', min(vals) if vals else 0); print('max:', max(vals) if vals else 0); print('mean:', sum(vals)/len(vals) if vals else 0); print('first 6:'); [print(' ', k, v) for k, v in list(d.items())[:6]]; log='/content/pareto_scan.log'; lines=open(log).readlines(); print('--- last 5 log lines:'); print(''.join(lines[-5:]))"}
