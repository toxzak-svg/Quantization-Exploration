{"code":"import os, time; t0 = time.time(); model_dir = '/content/models/gemma-4-E2B'; files = sorted(os.listdir(model_dir)); print('files:'); [print(' ', f, os.path.getsize(f'{model_dir}/{f}') if os.path.isfile(f'{model_dir}/{f}') else 'dir') for f in files]; total = sum(os.path.getsize(f'{model_dir}/{f}') for f in files if os.path.isfile(f'{model_dir}/{f}')); print(f'total: {total/1e9:.2f} GB'); wt = '/content/sub1quant/data/wiki.test.txt'; print(f'wikitext: {os.path.getsize(wt)/1e6:.2f} MB at {wt}')"}
